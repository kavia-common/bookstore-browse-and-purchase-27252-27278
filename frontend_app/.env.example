# PUBLIC_INTERFACE
# Vite environment variable for backend API base URL used by dev proxy.
# In development, vite.config.js will proxy /api to this target.
# Example for local dev if backend runs at :3001
VITE_API_BASE=http://localhost:3001
